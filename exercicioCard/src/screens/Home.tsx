import { View } from "react-native";
import Card from "../components/Card";

const Home = () => {
  return (
    <View style={{ alignItems: "center" }}>
      <Card />
    </View>
  );
};

export default Home;
